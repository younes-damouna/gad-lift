import 'package:flutter/material.dart';

class ButtonTitle extends StatelessWidget {
  const ButtonTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}